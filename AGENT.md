# distito — 基于 AI 会话的知识协作网络

> **Publish your thinking from your conversations. Build a knowledge network without context boundaries.**

---

## 一句话定位

**基于 AI 会话的知识协作网络。**

就像大模型蒸馏为小模型一样，distito 把 AI 对话进行压缩和提炼为可分享、可协作的知识，从而构建起一个没有上下文边界的 AI 知识协作网络。

在 AI 对话中输入 `=>` 加上你的编辑要求，AI 自动将对话内容提炼为知识节点发布到你的个人空间，供他人引用、衍生、协同。

---

## 核心价值

### 用户怎么说

> *"我打 `=>` 就把 AI 对话变成博客文章发出来了。"*

### 解决了什么问题

| 痛点 | distito 方案 |
|------|-------------|
| 好内容烂在对话历史里 | 打 `=>` 一键提炼发布，不离开对话 |
| 知识无法跨会话引用 | `=> URL` 引用任意知识注入当前上下文 |
| 个人品牌需要持续内容产出 | 每一次聊天的火花都成为可公开、可引用的知识节点 |
| 知识是孤立的 | inject 网络让知识可追溯、可衍生、可协作 |

---

## 核心流程

```
① 输入
   => [你的编辑要求]

   例如：
   => 自动按照人格化的方式总结内容并发布
   => 总结关键性的结论，不要说推演过程
   => 主要说明白CI/CD的核心原理，其他不要废话
   => 将以上内容总结为文章，字数控制在3000字以内

② AI 处理 → 展示文章预览
   ┌─────────────────────────────────────┐
   │ 📝 草稿                              │
   │                                     │
   │ # 标题                              │
   │                                     │
   │ 正文...                             │
   │                                     │
   │ ├─ 回复「确认」立即发布              │
   │ └─ 直接提修改意见，我帮你改          │
   └─────────────────────────────────────┘

③ 用户回复「确认」

④ 发布反馈
   ✅ 文章已发布 → https://distito.com/你的名字/文章-slug
```

### 流程要点

| 步骤 | 说明 |
|------|------|
| **触发** | `=>` 为固定前缀，后面跟自然语言编辑指令 |
| **预览修改** | 直接在聊天中提修改意见，不需要富文本编辑器 |
| **确认发布** | 回复「确认」立即发布，无二次弹窗 |
| **发布即公开** | 每篇文章有唯一公开 URL，展示在个人时间线 |

---

## `=>` 指令体系

`=>` 不是一个单一的 "一键发布"，而是一个**自然语言指令前缀**。指令可组合的维度：

| 维度 | 示例 | 说明 |
|------|------|------|
| **范围** | 上面这段 / 本次对话 / 最近3条 | 要处理哪段内容 |
| **风格** | 人格化 / 冷静客观 / 毒舌 / 技术严谨 | 语气和口吻 |
| **焦点** | 只说结论 / 推演过程 / 对比分析 / 核心原理 | 内容侧重 |
| **格式** | 3000字以内 / 列表 / 一段话 / Markdown | 输出形式 |
| **目标** | 发布到博客 / 先预览不发布 | 输出目的地（早期默认发博客） |
| **引用来源** | `=> <URL> 翻译为英文` | 在获取知识的同时直接下达处理指令 |

### 引用+指令 统一模式

`=> <URL>` 后加自然语言，将**知识注入**和**处理指令**合并为一步：

```
=> <url> 翻译为英文
   ① web fetch → 获取内容
   ② 注入上下文
   ③ AI 翻译 -> 输出结果
   ④ 用户确认后可发布

=> <url1>, <url2> 将两篇文章的观点融合到一起
   ① web fetch url1 + url2
   ② 同时注入上下文
   ③ AI 融合 -> 输出融合文章
   ④ 用户确认后可发布

=> <url> 总结为文章并发布到 tech base
   ① web fetch → 获取内容
   ② 注入上下文
   ③ AI 蒸馏 -> 预览
   ④ 用户确认 -> 发布（含 inject_from）
```

**核心变化**：以前需要两步（`=> <url>` 注入 → 再对话提要求），现在一步完成。URL 后的自然语言就是指令，注入和动作之间无需用户额外输入。

---

## 品牌

```
产品名：  distito
域名：    distito.com ✅ 可注册
Logo：    =>
定位：    基于 AI 会话的知识协作网络
一句话：  Build a knowledge network without context boundaries.
```

### 品牌名来源

**distito = distill（蒸馏）+ to（→）**

- `=>` 视觉上 = 箭头，语义上 = "变成"、"去向"
- "对话 `=>` 知识" = "对话蒸馏成可分享、可协作的知识"
- Logo 就是 `=>`，零设计成本

### 命名演变

```
markbot.wiki  →  原项目名，定位"人类和 AI 共享的知识清单"
arrow.*       →  方向转向"发布"时用 "→" 命名，但所有域名已被注册
distilto      →  distill + to 的合成，但拼写不顺
distito ✅    →  去掉多余的 l，简洁、朗朗上口
```

- **读音**：/dɪˈstɪtoʊ/（"迪斯蒂托"）
- **拆解**：`disti`（蒸馏） + `to`（去向、变成）
- **意义**：对话中的知识被蒸馏后，**去往**一个可分享、可协同的地方

### 与 markbot 的关系

distito 是 markbot 的方向重塑。markbot 最初定位为"人类和 AI 共享的知识清单"，方向是结构化知识管理（Forms + Items + 知识库 CRUD）。经过多轮讨论后重新定义为基于 AI 会话的知识协作网络，聚焦于从对话到可引用、可协作的知识网络。

---

## 产品架构（规划）

```
用户输入 `=> 把这段对话总结为文章发到博客`

         ↓
    1. 解析 `=>` 后面的自然语言指令
       └─ 范围识别 → 风格识别 → 内容要求
         ↓
    2. AI 执行内容转换（蒸馏）
       └─ 符合用户指定的每一次约束
         ↓
    3. 确认门（展示文章预览）
       └─ 用户确认 → 发布
       └─ 用户提修改意见 → 重新生成
         ↓
    4. 发布到 distito.com/用户/文章
       └─ 公开 URL，时间线归档
```

### 技术栈倾向

| 层 | 倾向 |
|---|------|
| 前端 | **SolidJS + StyleX**（系统指令既定） |
| 后端 API | Supabase Edge Functions（TypeScript/Deno） |
| 数据库 | PostgreSQL |
| MCP Server | Python（与 markbot 已有设计同源） |

---

## 后续待讨论

- [ ] distito.com 域名注册
- [ ] 文章页面风格（纯文字博客，后期加样式配置）
- [ ] 域名策略（统一二级域名 distito.com/用户名）
- [ ] 编辑修改功能（用户到官网中修改）
- [ ] 社交渠道自动同步（Twitter/Telegram等）
- [ ] MVP 范围划定

---

## 讨论历史

### 背景起源

本产品源于在 Kun 对话中聊到的 idea：通过 MCP 实现一个功能，在 AI 对话中输入 `=>` 就能将会话总结发布到社交媒体。

### 与 markbot 的关系

最初发现这个需求本质是 markbot（原项目名，位于 `/Users/free/Projects/markbot`）在原有知识清单定位基础上增加自动分享功能。经过多轮讨论后，产品方向从"AI 知识清单工具"转变为"AI 对话蒸馏分享平台"，因此新建 distito 项目。

### 商业评估摘要

- 不是独立 SaaS 平台级机会，而是个人品牌基础设施
- 独特生态位：市场上没有第二个产品让你在 AI 对话中打 `=>` 直接发布博客
- 付费意愿来自个人品牌投入（非工具需求）
- 先做自己的效率工具，包装成可分享的产品
